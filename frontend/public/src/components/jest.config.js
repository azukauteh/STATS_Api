module.exports = {
  // Specify the root directory for Jest to look for files
  roots: ["<rootDir>/frontend/public/src"],

  // Specify the test environment (e.g., jsdom for a browser-like environment)
  testEnvironment: "jest-environment-puppeteer"

  // Add any other Jest configuration options you need
};

